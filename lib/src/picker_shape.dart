/// Visual shape of a [HintedImagePicker] or [HintedMultiImagePicker].
enum PickerShape {
  /// Fully rounded — `size` acts as the diameter.
  circle,
  
  /// Rounded corners, radius controlled by `borderRadius`.
  rounded,
  
  /// Sharp corners.
  square }